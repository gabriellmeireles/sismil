<div>

    <div class="page-header">
        <div class="row align-items-center">
          <div class="col-md-6">
            <h2 class="page-title">{{$user->admin->rankDegree->short_name}} {{ $user->admin->war_name }} ( {{ $user->admin->combatArm->short_name }} )</h2>
            <div class="page-subtitle">
              <div class="row">
                <div class="col-auto">
                  <span class="text-reset">{{ $user->admin->section->short_name }} - {{ $user->userType->name }}</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

</div>
